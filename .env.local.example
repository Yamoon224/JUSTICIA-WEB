# Copier en .env.local (non versionné) et adapter.

# URL de l'API Laravel JUSTICIA, jamais exposée au navigateur : le frontend
# NextJS agit en BFF (Backend For Frontend) — voir src/lib/api/backend.ts.
BACKEND_URL=http://localhost:8000

NEXT_PUBLIC_APP_NAME=JUSTICIA
